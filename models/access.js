
exports.getAccessControl = function(key, secret) {

	var userKey = key
		,userSecret = secret;


		console.log('im the access and just stored some stuff like the key = ' + userKey + ' and the secret = ' + userSecret);

}